// 교수인ㅁ이 내주신 과제
int A, B;
printf("두 숫자를 입력해주세요: ");
scanf("%d %d", &A, &B);
if (A % B == 0){ //1. A가 B의 배수이면 , A/B출력
    printf("%d", A / B);
}
else if (A > B){ //2. A가 B보다 크고 배수가 아니라면 A%B
    printf("%d", A % B);
}
else if (A < B){ // 3. A가 B보다  작다면 , A+B
    printf("%d", A + B);
}
else { // 4. 아무것도 해당이 아니면 A,B 각각 출력
    printf("%d 와 %d", A, B);
}
